# ElastiTune Kibana Plugin Skeleton

This directory contains a starter Kibana plugin wrapper for launching ElastiTune from Kibana navigation.

Current scope:

- Registers an `ElastiTune` app entry
- Provides a launch link to the standalone ElastiTune control plane
- Serves as the first packaging step toward a native `Optimize this index` workflow
